import './Footer.css'

function Footer() {
    const handleChange = (event) => {
        setText(event.target.value);
      };
    return <footer>
    <p>Footer</p>
    </footer>
}

export default Footer